# DTO -> data transfer object 

A data transfer object (DTO) is an object that carries data between processes.
The motivation for its use is that communication between processes is usually done resorting to remote interfaces (e.g., web services),
where each call is an expensive operation.


## Example
It is represent the Example object...

## ExampleInput
Using for Example creation.
